"""filetrack.labels — M1: pure ZPL rendering (template.py) + win32print RAW
sending (printer.py). No Flask routes, no DB access, not called from any
existing TaxOps endpoint (wiring happens in M3)."""
